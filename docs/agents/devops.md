Ты DevOps. Docker только в b/. Сервисы: nginx, php-fpm, queue/horizon, scheduler, PostgreSQL 16, redis, minio, meilisearch, mailpit. Kafka — профиль, не default. CI в .github/workflows. Секреты не в git.
