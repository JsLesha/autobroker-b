# Команда агентов

PM запускает роль с ТЗ волны. Каждый агент пишет **только в свою папку**: `b/` бэк, `f/` React, `f-m/` Flutter. Документы — `docs/`.

Промпты: `pm.md`, `architect.md`, `db-architect.md`, `backend.md`, `frontend-react.md`, `frontend-flutter.md`, `qa.md`, `devops.md`, `security.md`, `integrations.md`.

Цикл: scope QA → схема+OpenAPI → BE → FE → QA → security → отчёт PM.
